// app/_middleware.ts
// import { NextResponse } from "expo-router";

// export function middleware(request: NextRequest) {
//   const isAuthenticated = checkAuth(request); // Custom auth logic
//   if (!isAuthenticated) {
//     return NextResponse.redirect("/login");
//   }
// }
